# ShipStation API Configuration (V1 API)
# Get your credentials from ShipStation: Settings > Account > API Settings
# Format: API Key and API Secret separated by colon, or use separate variables

# Option 1: Combined key:secret format
SHIPSTATION_API_KEY=your_api_key:your_api_secret

# Option 2: Separate key and secret (if your key doesn't contain a colon)
# SHIPSTATION_API_KEY=your_api_key
# SHIPSTATION_API_SECRET=your_api_secret

# Warehouse ID - numeric ID from ShipStation (not the se- prefixed ID)
# Find this in ShipStation: Settings > Shipping > Ship From Locations
SHIPSTATION_WAREHOUSE_ID=123456

# Optional: Webhook secret for verifying webhook signatures
SHIPSTATION_WEBHOOK_SECRET=your_webhook_secret

# Optional: Redis URL for rate caching (falls back to in-memory cache)
REDIS_URL=redis://localhost:6379

# Optional: Custom API URL (defaults to https://ssapi.shipstation.com)
# SHIPSTATION_API_URL=https://ssapi.shipstation.com
